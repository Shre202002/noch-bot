import { Card, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function AnalyticsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Chatbot Analytics</CardTitle>
        <CardDescription>
          This is where you will see analytics for your chatbot, including message counts and usage.
        </CardDescription>
      </CardHeader>
    </Card>
  );
}
